# AGENTS.md

## 基本方針

- このリポジトリで作業するエージェントは、既存の仕様書を優先して参照する。
- 方針に迷う場合は、実装前に `README.md` と `docs/` 配下の仕様書を確認する。
- MVPの範囲、技術選定、開発フェーズは、明示的な変更指示がない限り仕様書に従う。

## 日本語運用

- ドキュメント、仕様書、README、設計メモは日本語で書く。
- コミットメッセージは日本語で書く。
- Pull Requestのタイトルと本文は日本語で書く。
- Issue本文、リリースノート、変更説明も日本語で書く。
- コード識別子、ファイル名、APIパス、ライブラリ名、プロトコル名など、技術上英語であることが自然なものは英語のままでよい。

## Git運用

- `main` と `dev` へ直接pushしない。
- 変更は作業ブランチで行い、Pull Request経由で取り込む。
- 通常の開発PRは `dev` をベースブランチにする。
- リリースや本番反映のPRは `main` をベースブランチにする。
- `main` または `dev` に取り込む前に、PRブランチへ対象ブランチの最新状態を取り込む。
- 対象ブランチの最新状態を取り込んでいないPRは、更新してからマージする。

## 技術方針

- バックエンドは FastAPI + SQLModel + Alembic + PostgreSQL + uv を標準とする。
- バックエンドの依存関係管理と仮想環境管理には `uv` を使う。
- Poetry、Pipenv、場当たり的な `pip install` ワークフローは導入しない。
- フロントエンドは React + TypeScript + Vite を標準とする。
- API契約はOpenAPIスキーマを正とし、フロントエンドは生成TypeScriptクライアントを利用する。

## スコープ管理

- MVP完了前に公開共有を追加しない。
- MVP完了前に外部レシピURL解析を追加しない。
- 明示的に計画しない限り、MVP中にローカル画像保存をS3互換ストレージへ置き換えない。
- ブラウザ向けMVPが安定する前にネイティブモバイル実装を追加しない。
